# ConvoMind 🤖

ConvoMind is a lightweight AI chatbot built with Flask and Ollama.

## Features
- Local free LLM (Ollama)
- Session-based memory
- Web chat UI
- REST API
- Low RAM support

## Setup
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
